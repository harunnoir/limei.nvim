*limei.txt* *limei.nvim*      limei.nvim

==============================================================================
CONTENTS                                                        *limei-contents*

1. Introduction ................................................ |limei|
2. Setup .................................................. |limei-setup|
3. Options .............................................. |limei-options|
4. Commands ............................................ |limei-commands|

==============================================================================
INTRODUCTION                                                          *limei*

limei.nvim is a deeply dark, muted colorscheme for Neovim 0.10 and newer.
Its interface is emotionally neutral and quiet, without blue or cool-color
dominance. Major code categories have distinct semantic identities while
ordinary structure and plugin surfaces remain neutral.

==============================================================================
SETUP                                                            *limei-setup*

The colorscheme works without configuration: >

  :colorscheme limei
<
Optional Lua setup: >

  require("limei").setup({
    transparent = false,
    dim_inactive = true,
    terminal_colors = true,
    palette = {},
    roles = {},
    highlights = {},
  })
  vim.cmd.colorscheme("limei")
<
==============================================================================
OPTIONS                                                        *limei-options*

transparent       Remove active and inactive editor canvas backgrounds.
                  Floating windows and popup menus remain opaque.

dim_inactive      Recede inactive windows with a darker background. Default:
                  true.

terminal_colors   Install the muted 16-color terminal palette. Default: true.

styles            Optional attributes for comments, keywords, functions, and
                  variables.

palette           Palette overrides applied before highlights are generated.

roles             Semantic role overrides. Values may be palette keys or
                  #RRGGBB colors. Resolved after palette overrides.

highlights        A table or function returning final highlight overrides.

==============================================================================
COMMANDS                                                      *limei-commands*

There are no custom commands. Use |:colorscheme| and the Lua setup API.

vim:tw=78:ts=8:noet:ft=help:norl:
